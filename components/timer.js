import styles from "./timer.module.css"

const Timer = () => {
    
    return <div className={styles.container}>
        <h2 className={styles.title1}>Timer Box</h2>
        </div>
}

export default Timer